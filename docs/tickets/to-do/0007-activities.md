# 0007 — Activities (basic CRUD)

## Goal
Add activity records (e.g., tours, bookings, notes) that can be attached to itineraries later.

## Scope
- Prisma model: Activity.
- Shared Zod schemas.
- API endpoints for create/list/get/update/delete (auth required).

## Acceptance criteria
- Activities CRUD works for the current user.
- Tests for create/list/get/update/delete.
