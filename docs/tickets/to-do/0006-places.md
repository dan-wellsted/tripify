# 0006 — Places (basic CRUD)

## Goal
Add place records that can be attached to itineraries later.

## Scope
- Prisma model: Place.
- Shared Zod schemas.
- API endpoints for create/list/get/update/delete (auth required).

## Acceptance criteria
- Places CRUD works for the current user.
- Tests for create/list/get/update/delete.
